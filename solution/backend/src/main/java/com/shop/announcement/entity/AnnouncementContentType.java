package com.shop.announcement.entity;

/** Content type of a scrolling announcement (US-ANN-01 / FS-V11). */
public enum AnnouncementContentType {
    /** Text-only announcement. */
    TEXT,
    /** Image-only announcement. */
    IMAGE,
    /** Announcement with both text and image. */
    BOTH
}
