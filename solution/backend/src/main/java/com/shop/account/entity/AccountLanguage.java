package com.shop.account.entity;

/** Supported notification languages for a platform account (CS-10). */
public enum AccountLanguage {

    /** French — default language. */
    FR,

    /** English. */
    EN
}
