= Backlog — User Stories
:doctype: article
:toc: left
:toclevels: 3
:sectnums:
:icons: font
:source-highlighter: rouge

== Contexte

Ce document liste l'ensemble des user stories dérivées des fonctions système (FS) définies en SA et allouées en LA/PA. Chaque user story est tracée vers les éléments ARCADIA dont elle est issue.

Les user stories sont regroupées par domaine fonctionnel et ordonnées selon les dépendances d'implémentation (les stories d'infrastructure avant celles de fonctionnalité).

NOTE: Le statut de chaque story est à renseigner dans la colonne « Statut » au fil du développement. Valeurs suggérées : `À faire` / `En cours` / `Terminée`.

== Gestion des comptes (Admin)

_Fonctions ARCADIA : FS-A01, FS-A02, FS-A03 — UCSA-12 — LA : Console Administrateur / Service Compte_

[cols="1,3,4,1", options="header"]
|===
| ID | User story | Critères d'acceptation | Statut

| *US-ADM-01*
| En tant qu'administrateur, je peux créer un compte acheteur ou vendeur depuis la console d'administration.
a|
- [x] Formulaire : email, rôle (acheteur / vendeur), informations de profil (sans champ mot de passe)
- [x] Erreur si l'email est déjà utilisé (pas de doublon)
- [x] Compte créé avec statut `en_attente_activation`
- [x] Le système envoie automatiquement un email à l'adresse saisie contenant un lien d'activation
- [x] Le lien permet à l'utilisateur de définir son mot de passe et d'activer son compte (même flux que UCSA-01)
- [ ] Le lien est à usage unique et expire après 24 h ; l'admin peut renvoyer un lien depuis la fiche utilisateur
| Terminée

| *US-ADM-02*
| En tant qu'administrateur, je peux suspendre un compte actif (acheteur ou vendeur).
a|
- [x] Action disponible uniquement sur les comptes au statut `actif`
- [x] Compte passe en statut `suspendu` ; l'utilisateur ne peut plus se connecter
- [x] Action réversible (voir US-ADM-03)
| Terminée

| *US-ADM-03*
| En tant qu'administrateur, je peux réactiver un compte suspendu.
a|
- [x] Action disponible uniquement sur les comptes au statut `suspendu`
- [x] Compte repasse en statut `actif` ; l'utilisateur peut de nouveau se connecter
| Terminée

| *US-ADM-04*
| En tant qu'administrateur, je peux supprimer un compte (actif ou suspendu).
a|
- [x] Confirmation obligatoire avant suppression
- [x] Compte passe en statut `supprimé`
- [x] L'utilisateur ne peut plus se connecter ni récupérer son compte
| Terminée

| *US-ADM-05*
| En tant qu'administrateur, je peux modifier le rôle d'un utilisateur (acheteur ↔ vendeur).
a|
- [x] Sélecteur de rôle disponible sur la fiche utilisateur
- [x] Modification effective immédiatement
| Terminée

| *US-ADM-06*
| En tant qu'administrateur, je peux ajouter un transporteur avec son nom, les pays de la zone euro couverts et l'URL de son site de suivi.
a|
- [x] Formulaire : nom, pays de la zone euro couverts (multi-sélect restreint à la zone euro), URL de suivi
- [x] Transporteur créé avec statut `actif` et disponible immédiatement dans le back-office vendeur
| Terminée

| *US-ADM-07*
| En tant qu'administrateur, je peux modifier les informations d'un transporteur existant.
a|
- [x] Tous les champs sont éditables (nom, pays, URL de suivi)
- [x] Modifications effectives immédiatement
| Terminée

| *US-ADM-08*
| En tant qu'administrateur, je peux désactiver un transporteur sans le supprimer.
a|
- [x] Transporteur masqué du back-office vendeur pour les nouvelles expéditions
- [x] Les expéditions existantes référençant ce transporteur ne sont pas affectées
| Terminée

| *US-ADM-09*
| En tant qu'administrateur, je peux supprimer un transporteur qui n'est référencé dans aucune expédition active.
a|
- [x] Suppression bloquée si des expéditions actives référencent ce transporteur, avec proposition de désactivation
- [x] Confirmation obligatoire avant suppression effective
| Terminée

| *US-ADM-10*
| En tant qu'administrateur, je peux activer ou désactiver le mode maintenance de la boutique.
a|
- [x] Bascule « Mode maintenance » disponible dans la console d'administration (UCSA-14 / FS-A05)
- [x] En mode maintenance : toute navigation sur le portail acheteur et le back-office vendeur affiche une page « Site en maintenance »
- [x] Les administrateurs authentifiés conservent l'accès à la console sans interruption
- [x] L'état du mode maintenance est persisté en base (`platform_settings`) et survit au redémarrage
- [x] Le changement d'état prend effet immédiatement sans redémarrage
| Terminée

|===

== Inscription et activation de compte

_Fonctions ARCADIA : FS-B01, FS-B10, CS-07 — UCSA-01 — LA : Service Compte / Service Notification / Adaptateur Messagerie_

[cols="1,3,4,1", options="header"]
|===
| ID | User story | Critères d'acceptation | Statut

| *US-REG-01*
| En tant que visiteur, je peux créer un compte acheteur en saisissant mon email, mon mot de passe et mes informations de profil.
a|
- [x] Formulaire : email, mot de passe (+ confirmation), nom, prénom
- [x] Erreur si l'email est déjà utilisé
- [x] Compte créé en statut `en_attente_activation`
- [x] Email d'activation envoyé automatiquement via SendGrid (CS-07)
| Terminée

| *US-REG-02*
| En tant que nouvel acheteur, je peux activer mon compte en cliquant sur le lien reçu par email.
a|
- [x] Token UUID à usage unique, TTL 24 h (CS-07)
- [x] Clic valide → compte passe en statut `actif`, redirection vers la page de connexion
- [x] Token expiré (> 24 h) → message d'erreur + proposition de renvoi du lien
- [x] Token déjà utilisé → message « votre compte est déjà actif »
| Terminée

| *US-REG-03*
| En tant que nouvel acheteur dont le token a expiré, je peux demander le renvoi d'un nouveau lien d'activation.
a|
- [x] Lien « Renvoyer le lien » accessible depuis la page d'erreur de token expiré
- [x] Nouveau token généré ; l'ancien est invalidé
- [x] Email d'activation renvoyé
| Terminée

|===

== Gestion du profil

_Fonctions ARCADIA : FS-B02, FS-B11, FS-B13, FS-V01, FS-V15 — UCSA-02 — LA : Service Compte_

[cols="1,3,4,1", options="header"]
|===
| ID | User story | Critères d'acceptation | Statut

| *US-PRF-01*
| En tant qu'acheteur authentifié, je peux modifier mes informations personnelles (nom, adresse de livraison par défaut, mot de passe).
a|
- [x] Formulaire pré-rempli avec les données actuelles
- [x] Unicité de l'email vérifiée si modification de l'email
- [x] Confirmation de succès après sauvegarde
| Terminée

| *US-PRF-02*
| En tant que vendeur authentifié, je peux modifier mes informations personnelles (nom, adresse, mot de passe, coordonnées).
a|
- [x] Même règles que US-PRF-01
| Terminée

| *US-PRF-03*
| En tant qu'acheteur authentifié, je peux gérer un carnet d'adresses de livraison : ajouter une nouvelle adresse, modifier ou supprimer une adresse existante, et en définir une comme adresse par défaut.
a|
- [x] Section « Mes adresses » accessible depuis l'espace compte
- [x] Formulaire d'ajout : libellé (ex. « Domicile »), nom du destinataire (peut différer du nom du compte), rue, ville, code postal, pays (zone euro uniquement — CS-04)
- [x] L'adresse par défaut est présélectionnée automatiquement au passage de commande
- [x] Une seule adresse peut être marquée par défaut à la fois
- [x] Suppression logique (soft-delete) bloquée si c'est la seule adresse active du carnet
- [x] Minimum 1 adresse active, pas de maximum imposé
- [x] Tunnel d'achat : sélection parmi le carnet (addressId) au lieu de saisie inline — commande référence delivery_address par FK
| Terminée

| *US-PRF-04*
| En tant qu'acheteur, je peux choisir à l'inscription et modifier depuis mon compte si j'accepte de recevoir des emails commerciaux du vendeur.
a|
- [x] Case à cocher opt-in non pré-cochée dans le formulaire d'inscription, libellé : « J'accepte de recevoir des offres et actualités commerciales »
- [x] Même case disponible dans l'onglet « Mon compte » (état actuel pré-rempli)
- [x] Modification enregistrée immédiatement avec horodatage
- [x] Le retrait du consentement n'affecte pas l'accès à la boutique ni les emails transactionnels
| Terminée

| *US-PRF-05*
| En tant que vendeur, je peux exporter au format CSV la liste des acheteurs ayant donné leur consentement marketing, afin de l'importer dans mon outil de mailing.
a|
- [x] Bouton « Exporter la liste mailing » dans le back-office vendeur
- [x] Le fichier CSV contient : email, prénom, nom des acheteurs avec `marketing_consent = true`
- [x] Seuls les comptes actifs et consentants sont inclus
- [x] L'export est journalisé (date, nombre de contacts exportés)
| Terminée

|===

== Catalogue produits — Vendeur

_Fonctions ARCADIA : FS-V02, FS-V03, FS-V12, FS-V13, FS-V14 — UCSA-10 — LA : Service Catalogue_

[cols="1,3,4,1", options="header"]
|===
| ID | User story | Critères d'acceptation | Statut

| *US-CAT-01*
| En tant que vendeur, je peux créer une fiche produit avec nom, description, prix HT, photos, catégorie, quantité disponible et seuil d'alerte de stock.
a|
- [x] Champs obligatoires : nom, prix HT, quantité disponible
- [x] Produit créé en statut `publié` par défaut
- [x] Photos (URLs) uploadées et associées au produit
| Terminée

| *US-CAT-02*
| En tant que vendeur, je peux modifier une fiche produit existante.
a|
- [x] Tous les champs sont éditables
- [x] Modifications visibles immédiatement dans le catalogue acheteur
| Terminée

| *US-CAT-03*
| En tant que vendeur, je peux archiver un produit pour le retirer du catalogue sans le supprimer.
a|
- [x] Produit archivé non visible dans le catalogue acheteur
- [ ] Archivage bloqué si le produit est référencé dans une commande active — implémentation différée à US-ORD (domaine commandes non encore créé)
| Terminée

| *US-CAT-04*
| En tant que vendeur, je peux consulter et mettre à jour les niveaux de stock des produits du catalogue partagé et définir un seuil d'alerte.
a|
- [x] Stock et seuil d'alerte affichés dans la liste des produits
- [x] Quantités modifiables depuis le formulaire produit et via `PATCH /stock`
- [x] Indicateur visuel (couleur) sur les produits sous le seuil ou en rupture
| Terminée

| *US-CAT-05*
| En tant que vendeur, je reçois une notification lorsque le stock d'un produit passe en dessous de son seuil d'alerte.
a|
- [x] Notification déclenchée automatiquement lors du franchissement du seuil (table `stock_alerts`)
- [x] Indicateur visible dans le back-office vendeur (panneau alertes + badge sur nav)
| Terminée

| *US-CAT-08*
| En tant que vendeur, je peux mettre à jour les niveaux de stock de plusieurs produits simultanément depuis la vue liste du catalogue.
a|
- [x] La vue liste affiche une colonne « Stock » éditable en ligne pour chaque produit
- [x] Le vendeur peut modifier les quantités de plusieurs produits sans quitter la liste
- [x] Un bouton « Enregistrer les modifications » soumet toutes les mises à jour en une seule requête (`PATCH /api/vendor/products/stocks`)
- [x] Seules les lignes modifiées sont envoyées au serveur
- [x] Les alertes de rupture sont réévaluées après enregistrement (FS-V03)
- [x] En cas d'erreur partielle, les lignes en succès sont appliquées et les erreurs sont signalées
| Terminée

| *US-CAT-06*
| En tant que vendeur, je peux importer des produits ou mettre à jour leur stock depuis un fichier CSV afin de créer ou réapprovisionner plusieurs fiches en une seule opération.
a|
- [x] Un bouton « Importer CSV » est disponible dans la section gestion du catalogue du back-office vendeur
- [x] Le fichier CSV respecte un en-tête défini : `id,nom,description,prix,categorie,quantite,seuil_alerte` ; l'en-tête de l'export (avec colonne `statut` supplémentaire) est également accepté pour permettre le cycle export → réimport
- [x] Si la colonne `id` est renseignée sur une ligne, seul le stock du produit existant est mis à jour (`quantite` et `seuil_alerte`) ; les autres champs sont ignorés
- [x] Si la colonne `id` est vide, une nouvelle fiche produit est créée ; les colonnes obligatoires sont alors `nom` et `prix`
- [x] Le système valide le format de l'en-tête avant traitement ; en cas d'en-tête invalide, l'import est rejeté en totalité
- [x] Un rapport ligne par ligne est affiché après traitement : créé / mis à jour / erreur avec message explicite
- [x] Les lignes valides sont traitées même si d'autres lignes sont en erreur (import partiel)
- [x] L'import est accessible uniquement aux comptes vendeur authentifiés (FS-V02 / CS-01)
| Terminée

| *US-CAT-07*
| En tant que vendeur, je peux exporter le catalogue produits partagé au format CSV afin de le consulter ou le réimporter ultérieurement.
a|
- [x] Un bouton « Exporter CSV » est disponible dans la section gestion du catalogue du back-office vendeur
- [x] Le fichier généré contient toutes les fiches produits (publiées et archivées) avec les colonnes : `id,nom,description,prix,categorie,quantite,seuil_alerte,statut` (la colonne `id` permet de réutiliser le fichier pour une mise à jour de stock via l'import CSV)
- [x] Le fichier est téléchargé directement par le navigateur avec le nom `catalogue_export_YYYYMMDD.csv`
- [x] L'encodage du fichier est UTF-8 avec BOM pour compatibilité tableur
- [x] L'export est accessible uniquement aux comptes vendeur authentifiés (FS-V02 / CS-01)
| Terminée

| *US-CAT-09*
| En tant que vendeur, je peux téléverser des fichiers images pour les photos de mes produits afin de ne pas avoir à héberger moi-même les images.
a|
- [x] Un bouton « Ajouter une photo » est disponible dans le formulaire de création et d'édition d'un produit
- [x] Le vendeur sélectionne un fichier image local (JPEG, PNG, GIF ou WebP) ; le fichier est envoyé au serveur qui le stocke et retourne une URL publique
- [x] L'URL retournée est automatiquement ajoutée à la liste des photos du produit
- [x] Les photos sont affichées sous forme de miniatures avec bouton de suppression individuel
- [x] Le serveur valide le type MIME déclaré **et** le contenu réel du fichier ; tout fichier non conforme est rejeté avec HTTP 400
- [x] La taille maximale acceptée est 5 Mo par fichier
- [x] Les fichiers sont renommés en UUID côté serveur pour éviter les collisions et les attaques path traversal
- [x] Les images sont servies publiquement par le backend sous `/uploads/products/**` (accessibles sans authentification pour l'affichage dans le catalogue acheteur)
- [x] L'upload est accessible uniquement aux comptes vendeur authentifiés (FS-V16 / CS-01)
| Terminée

|===

== Catalogue produits — Acheteur

_Fonctions ARCADIA : FS-B03 — UCSA-03 — LA : Service Catalogue_

[cols="1,3,4,1", options="header"]
|===
| ID | User story | Critères d'acceptation | Statut

| *US-SHP-01*
| En tant que visiteur (connecté ou non), je peux parcourir les produits publiés du catalogue.
a|
- [x] Liste paginée des produits avec nom, prix HT/TTC, disponibilité
- [x] Accès sans authentification
| Terminée

| *US-SHP-02*
| En tant qu'acheteur, je peux filtrer les produits par catégorie, prix maximum et disponibilité, et effectuer une recherche textuelle.
a|
- [x] Filtres cumulables, résultats mis à jour dynamiquement
- [x] Message « aucun résultat » avec option de réinitialisation des filtres
| Terminée

| *US-SHP-03*
| En tant qu'acheteur authentifié, je peux m'abonner à une alerte email pour un produit indisponible et être notifié dès qu'il est de nouveau en stock.
a|
- [x] Bouton « M'alerter quand disponible » visible uniquement sur les produits avec `quantity == 0`
- [x] L'acheteur non connecté est invité à se connecter avant de pouvoir s'abonner
- [x] Impossible de s'abonner si le produit est déjà en stock (erreur explicite)
- [x] Impossible de s'abonner deux fois au même produit (erreur explicite)
- [x] L'acheteur reçoit un email dans sa langue de préférence (FR / EN / ES) dès que le vendeur remet le produit en stock
- [x] L'abonnement est supprimé automatiquement après l'envoi de l'email (implémenté par suppression plutôt que flag — comportement équivalent)
- [x] Onglet « Mes alertes » dans l'espace compte : liste des abonnements actifs, possibilité d'en annuler
- [x] La suppression de l'abonnement retire l'alerte sans envoyer d'email
| Terminée

|===

== Panier

_Fonctions ARCADIA : FS-B04, FS-B05, CS-03 — UCSA-04 — LA : Service Panier_

[cols="1,3,4,1", options="header"]
|===
| ID | User story | Critères d'acceptation | Statut

| *US-CRT-01*
| En tant qu'acheteur authentifié, je peux ajouter des produits à mon panier, modifier les quantités et retirer des articles.
a|
- [x] Ajout bloqué si le produit est en rupture de stock (message explicite)
- [x] Récapitulatif (lignes + total) mis à jour en temps réel
| Terminée

| *US-CRT-02*
| En tant qu'acheteur authentifié, mon panier est automatiquement sauvegardé et intégralement restauré à ma reconnexion.
a|
- [x] Panier persisté en base après chaque modification (CS-03)
- [x] Contenu (produits, quantités) restauré à la reconnexion suivante
| Terminée

|===

== Tunnel d'achat et paiement

_Fonctions ARCADIA : FS-B06, FS-B07, FS-P01, FS-P02, CS-02, CS-04 — UCSA-05 — LA : Service Commande / Service Paiement / Adaptateur Paiement_

[cols="1,3,4,1", options="header"]
|===
| ID | User story | Critères d'acceptation | Statut

| *US-ORD-01*
| En tant qu'acheteur, je peux saisir une adresse de livraison restreinte aux pays de la zone euro.
a|
- [x] Liste déroulante limitée aux pays de la zone euro (CS-04)
- [x] Adresse hors zone euro bloquée avec message d'erreur
| Terminée

| *US-ORD-02*
| En tant qu'acheteur, je peux choisir un mode de livraison parmi les transporteurs actifs couvrant mon pays de destination.
a|
- [x] Seuls les transporteurs actifs et compatibles avec le pays de livraison sont proposés
- [x] Tarif affiché pour chaque option
| Terminée

| *US-ORD-03*
| En tant qu'acheteur, je peux régler ma commande par carte bancaire avec autorisation immédiate via Stripe.
a|
- [x] Données carte transmises directement à Stripe sans transit en clair par le backend (PCI-DSS)
- [x] Commande confirmée et tous les vendeurs notifiés par email si la transaction est autorisée ; stock réservé
- [x] Message d'erreur explicite si la transaction est refusée
| Terminée

| *US-ORD-04*
| En tant qu'acheteur, je peux régler ma commande par virement bancaire ; je reçois les coordonnées bancaires du vendeur et ma commande est placée en attente de confirmation.
a|
- [x] Commande créée en statut `paiement_en_attente_virement`
- [x] Coordonnées bancaires et référence à inclure affichées et envoyées par email
- [x] Stock pré-réservé ; tous les vendeurs notifiés par email du virement en attente
| Terminée

| *US-ORD-05*
| En tant qu'acheteur, je reçois un email de confirmation après validation de ma commande.
a|
- [x] Email envoyé via SendGrid contenant le numéro de commande, le récapitulatif et la livraison estimée
| Terminée

|===

== Identité visuelle — Vendeur

_Fonctions ARCADIA : FS-V16 — UCSA-16 — LA : Service Compte / Service Paramètres_

[cols="1,3,4,1", options="header"]
|===
| ID | User story | Critères d'acceptation | Statut

| *US-VIS-01*
| En tant que vendeur, je peux personnaliser l'identité visuelle de ma boutique (logo, bannière hero, couleur d'accentuation et couleur de fond) depuis mon back-office.
a|
- [x] Upload logo (JPEG, PNG, WebP ≤ 5 Mo) via `POST /api/me/logo` ; logo affiché dans l'en-tête des deux portails
- [x] Suppression logo via `DELETE /api/me/logo` ; icône par défaut restaurée
- [x] Upload bannière hero (JPEG, PNG, WebP ≤ 5 Mo) via `POST /api/me/banner` ; bannière affichée en haut de la page d'accueil acheteur
- [x] Suppression bannière via `DELETE /api/me/banner` ; section hero masquée
- [x] Sélecteur + saisie hexadécimale pour la couleur d'accentuation (`--accent`) ; valeur persistée dans `platform_settings` (clé `shop_accent_color`, défaut `#4e8b82`)
- [x] Sélecteur + saisie hexadécimale pour la couleur de fond (`--bg`) ; valeur persistée dans `platform_settings` (clé `shop_bg_color`, défaut `#f2f6f5`)
- [x] Bouton « Réinitialiser » individuel pour chaque couleur ; restaure la valeur par défaut
- [x] Un seul bouton « Enregistrer » sauvegarde les deux couleurs simultanément
- [x] `GET /api/public/theme` retourne `{ logoUrl, bannerUrl, accentColor, bgColor }` ; le portail acheteur applique les variables CSS `--accent`, `--accent-hover` et `--bg` au démarrage
- [x] Formats non supportés pour les images → message d'erreur ; image courante conservée
- [x] Toute modification est répercutée immédiatement sans interruption de service
| Terminée

|===

== Gestion des commandes — Vendeur

_Fonctions ARCADIA : FS-V04, FS-V09 — UCSA-07, UCSA-13 — LA : Service Commande / Service Paiement_

[cols="1,3,4,1", options="header"]
|===
| ID | User story | Critères d'acceptation | Statut

| *US-VND-01*
| En tant que vendeur, je suis notifié des nouvelles commandes et je peux les consulter dans mon tableau de bord.
a|
- [x] Notification email à chaque nouvelle commande
- [x] Commande visible dans le back-office avec détail acheteur, lignes et statut
| Terminée

| *US-VND-02*
| En tant que vendeur, je peux confirmer la réception d'un virement bancaire pour déclencher la prise en charge de la commande.
a|
- [x] Liste des commandes en statut `paiement_en_attente_virement` dans le back-office
- [x] « Confirmer » → commande passe en `en_attente` ; acheteur notifié
- [x] « Rejeter » → commande annulée ; stock pré-réservé libéré ; acheteur notifié
| Terminée

|===

== Expédition et suivi

_Fonctions ARCADIA : FS-V05, FS-B08, FS-C01, CS-05 — UCSA-07, UCSA-08 — LA : Service Expédition_

[cols="1,3,4,1", options="header"]
|===
| ID | User story | Critères d'acceptation | Statut

| *US-EXP-01*
| En tant que vendeur, je peux déclarer l'expédition d'une commande en saisissant le numéro de suivi récupéré sur le reçu de dépôt physique auprès du transporteur.
a|
- [x] Formulaire : sélecteur de transporteur (actifs uniquement) + champ numéro de suivi (obligatoire)
- [x] Soumission bloquée si le champ numéro de suivi est vide
- [x] Commande passe en statut `expédiée` ; numéro de suivi enregistré en base
- [x] Acheteur notifié par email avec le numéro de suivi et le lien vers le site du transporteur
| Terminée

| *US-EXP-02*
| En tant qu'acheteur, je peux consulter le numéro de suivi de ma commande et accéder au site du transporteur depuis mon espace commandes.
a|
- [x] Numéro de suivi affiché dans le détail de la commande expédiée
- [x] Lien vers le site de suivi du transporteur (URL construite à partir du numéro et de l'URL configurée par l'admin)
- [x] Si numéro non encore disponible : message « en cours d'expédition — numéro non disponible »
| Terminée

|===

== Annulation de commande

_Fonctions ARCADIA : FS-B09, FS-V08, FS-P03 — UCSA-06 — LA : Service Commande / Service Paiement_

[cols="1,3,4,1", options="header"]
|===
| ID | User story | Critères d'acceptation | Statut

| *US-CAN-01*
| En tant qu'acheteur, je peux annuler une commande en attente ou en préparation depuis mon espace commandes sans que le vendeur puisse s'y opposer.
a|
- [x] Bouton d'annulation disponible sur les commandes aux statuts `en_attente` et `en_préparation`
- [x] Confirmation obligatoire avant annulation
- [x] Si paiement par virement : champ « IBAN de remboursement » obligatoire dans le formulaire d'annulation, avec validation du format IBAN
- [x] Si paiement par carte : aucun champ bancaire requis
- [x] Annulation effective immédiatement ; stock libéré ; vendeur notifié
| Terminée

| *US-CAN-02*
| En tant que vendeur, je suis notifié d'une annulation avant expédition et dois procéder au remboursement si le paiement était par virement.
a|
- [x] Notification de l'annulation dans le back-office (lecture seule — aucun refus possible)
- [x] Paiement carte → remboursement déclenché automatiquement par le système via Stripe (FS-P03) ; aucune action requise du vendeur
- [x] Paiement virement → IBAN de l'acheteur et montant affichés au vendeur pour virement inverse ; commande en `remboursement_virement_en_cours`
| Terminée

| *US-CAN-03*
| En tant que vendeur, je peux accepter une annulation après expédition en exigeant le renvoi préalable du colis.
a|
- [x] Commande passe en statut `en_attente_retour` ; acheteur notifié du renvoi requis
- [x] Vendeur confirme la réception du colis retourné dans le back-office
- [x] Remboursement carte → déclenché via Stripe (FS-P03) après confirmation de réception
- [x] Remboursement virement → commande passe en `remboursement_virement_en_cours` ; IBAN + montant fournis au vendeur pour virement inverse
| Terminée

| *US-CAN-04*
| En tant que vendeur, je peux accepter une annulation après expédition sans exiger le retour du colis.
a|
- [x] Paiement carte → remboursement déclenché immédiatement via Stripe (FS-P03) ; commande passe en `annulée` ; acheteur notifié
- [x] Paiement virement → commande passe en `remboursement_virement_en_cours` ; IBAN + montant fournis au vendeur pour virement inverse
| Terminée

| *US-CAN-05*
| En tant que vendeur, je peux confirmer l'émission d'un virement inverse de remboursement vers l'acheteur.
a|
- [x] Bouton « Confirmer le virement de remboursement » accessible depuis la commande en statut `remboursement_virement_en_cours`
- [x] Confirmation → commande passe en `annulée` (FS-V10) ; acheteur notifié que le virement de remboursement a été émis
- [x] IBAN de l'acheteur et montant rappelés sur l'écran de confirmation
| Terminée

| *US-CAN-06*
| En tant qu'acheteur, je peux demander l'annulation d'une commande déjà expédiée et je suis informé de la décision du vendeur.
a|
- [x] Bouton « Demander l'annulation » disponible sur les commandes au statut `expédiée` (FS-B09 / UCSA-06)
- [x] Formulaire avec champ motif obligatoire ; si paiement par virement, saisie obligatoire de l'IBAN de remboursement
- [x] Commande passe en statut `annulation_demandée_après_expédition` ; vendeur notifié (FS-V08)
- [x] L'acheteur est notifié par email de la décision du vendeur : acceptation (avec ou sans condition de renvoi — US-CAN-03/04) ou refus
| Terminée

|===

== Rapports de ventes

_Fonctions ARCADIA : FS-V07 — UCSA-11 — LA : Service Rapport_

[cols="1,3,4,1", options="header"]
|===
| ID | User story | Critères d'acceptation | Statut

| *US-RPT-01*
| En tant que vendeur, je peux consulter les indicateurs de ventes sur une période donnée et exporter les données.
a|
- [x] Indicateurs : chiffre d'affaires, nombre de commandes, panier moyen, taux d'annulation
- [x] Classement des produits les plus vendus (dérivé des `LigneCommande`)
- [x] Filtres par période et par catégorie
- [x] Export CSV téléchargeable côté client
| Terminée

|===

== Annonces défilantes

_Fonctions ARCADIA : FS-V11 — UCSA-15 — LA : Service Annonce / Back-office Vendeur / Portail Acheteur_

[cols="1,3,4,1", options="header"]
|===
| ID | User story | Critères d'acceptation | Statut

| *US-ANN-01*
| En tant que vendeur, je peux créer, modifier, réordonner et supprimer des annonces défilantes affichées sur la page d'accueil de la boutique.
a|
- [x] Formulaire de création : type de contenu (texte / image / les deux), champ texte (max 500 caractères), URL d'image optionnelle, lien de redirection optionnel, ordre d'affichage (FS-V11)
- [x] Les champs obligatoires varient selon le type : texte seul → champ texte requis ; image seule → image requise ; mixte → les deux requis
- [x] Lorsqu'une URL d'image est saisie, l'application détecte automatiquement l'orientation de l'image (portrait si hauteur ≥ largeur, paysage si largeur > hauteur) et l'affiche sous forme de badge avant enregistrement
- [x] L'orientation détectée est transmise au serveur et stockée avec l'annonce ; le vendeur voit l'orientation dans la liste du back-office
- [x] Le carrousel de la page d'accueil adapte la mise en page selon l'orientation : images paysage en pleine largeur, images portrait en format réduit centré
- [x] Les annonces actives sont affichées dans le carrousel de la page d'accueil par ordre croissant
- [x] Un clic sur une annonce avec lien redirige l'internaute vers ce lien
- [x] Le vendeur peut modifier une annonce existante depuis la liste du back-office
- [x] Le vendeur peut réordonner les annonces (boutons ↑ / ↓)
- [x] La suppression est immédiate et retire l'annonce du carrousel
- [x] Si aucune annonce n'est définie, le carrousel est masqué sur la page d'accueil
| Terminée

|===

== Sécurité (ANSSI)

_Fonctions ARCADIA : FS-S01..S13 — Contraintes SA : CS-11..CS-21 — Contraintes PA : CPA-09..CPA-17 — LA : Service Sécurité / Service Journalisation_

[cols="1,3,4,1", options="header"]
|===
| ID | User story | Critères d'acceptation | Statut

| *US-SEC-01*
| En tant qu'utilisateur, ma session expire après 30 minutes d'inactivité et ma déconnexion révoque mon jeton côté serveur.
a|
- [x] Le jeton JWT est stocké dans un cookie `HttpOnly; Secure; SameSite=Strict` (plus de `localStorage`) (FS-S03 / CS-13)
- [x] La durée de vie du jeton est de 30 minutes glissants ; chaque requête active réinitialise le compteur
- [x] Une déconnexion explicite (bouton « Se déconnecter ») inscrit le jeton dans la liste noire Redis et retourne une réponse `401` à toute requête ultérieure avec ce jeton
- [x] Un jeton expiré ou révoqué retourne `401 Unauthorized` sur tous les endpoints protégés
| Terminée

| *US-SEC-02*
| En tant que développeur, les requêtes d'état sont protégées contre les attaques CSRF et les cookies de session portent les attributs de sécurité requis.
a|
- [x] La configuration Nginx (`nginx.conf` production) applique `SameSite=Strict` sur les `Set-Cookie` (FS-S05 / CS-14 / CPA-12)
- [x] Les cookies de session portent les attributs `Secure`, `HttpOnly` et `SameSite=Strict`
- [x] Tout appel cross-origin non autorisé sur un endpoint d'état est rejeté avec `403 Forbidden`
| Terminée

| *US-SEC-03*
| En tant qu'ops, toutes les communications avec la plateforme sont chiffrées en TLS 1.2 minimum avec redirection automatique HTTP → HTTPS.
a|
- [x] Nginx (`nginx.conf` production) configuré en HTTPS sur le port 443, redirection 301 HTTP → HTTPS (FS-S06 / CS-13 / CPA-09)
- [x] Protocoles autorisés : TLS 1.2 et TLS 1.3 uniquement (`ssl_protocols TLSv1.2 TLSv1.3`)
- [x] SSLv3, TLS 1.0 et TLS 1.1 explicitement désactivés (`ssl_protocols` sans ces versions)
- [x] Certificat TLS émis par une CA reconnue (Let's Encrypt), renouvelé automatiquement via service Certbot dans docker-compose.yml
- [x] En-tête `Strict-Transport-Security` présent sur toutes les réponses (HSTS, max-age=31536000 + preload)
| Terminée

| *US-SEC-04*
| En tant qu'administrateur, je peux révoquer les mots de passe d'un groupe d'utilisateurs suspects et les comptes non renouvelés dans les 24 h sont désactivés automatiquement.
a|
- [x] Endpoint d'administration `POST /api/admin/accounts/revoke-passwords` permettant de marquer un ensemble de comptes (par rôle ou liste d'emails) avec `password_revoked = true` (FS-S11 / CS-18 / CPA-17)
- [x] Les utilisateurs concernés reçoivent un email les invitant à réinitialiser leur mot de passe immédiatement
- [x] Un job `@Scheduled` toutes les heures désactive automatiquement les comptes dont le mot de passe n'a pas été renouvelé 24 h après la révocation (`statut = SUSPENDED`)
- [x] L'administrateur dispose d'une vue listant les comptes en état « révoqué en attente de renouvellement » avec leur ancienneté
| Terminée

| *US-SEC-05*
| En tant qu'administrateur, je peux consulter et filtrer les journaux d'audit de sécurité depuis la console d'administration.
a|
- [x] Section « Journaux d'audit » accessible depuis la console admin uniquement (rôle ADMIN — FS-S07 / CS-15 / CPA-13)
- [x] Affichage paginé : horodatage UTC, type d'événement, identifiant utilisateur, adresse IP, détails
- [x] Filtres par type d'événement, période (from/to), utilisateur (substring), IP (exacte)
- [x] Export CSV des journaux filtrés téléchargeable depuis le navigateur
- [x] Tout accès depuis un compte non-ADMIN retourne `403 Forbidden`
| Terminée

| *US-SEC-06*
| En tant qu'ops, les journaux de sécurité sont conservés au minimum 12 mois et protégés contre toute suppression ou modification non autorisée.
a|
- [x] La table `audit_log` est partitionnée mensuellement (PostgreSQL `PARTITION BY RANGE (occurred_at)`) ; purge automatique après 13 mois via `@Scheduled` job (FS-S07 / CS-15 / CPA-13)
- [x] Le compte applicatif utilise `SELECT + INSERT` uniquement sur `audit_log` — `REVOKE UPDATE, DELETE` documenté dans le schéma pour application en production par un DBA
- [x] Rôle `shop_maintenance` créé avec tous les droits sur `audit_log` pour la maintenance (DDL, archivage)
- [x] L'intégrité des partitions est vérifiable via `GET /api/admin/audit-logs/partitions` (row count + disk size par partition depuis `pg_class`)
| Terminée

| *US-SEC-07*
| En tant que développeur, les données sensibles stockées en base (secrets TOTP) sont chiffrées au niveau colonne.
a|
- [x] La colonne `totp_secret` est chiffrée via un `AttributeConverter` JPA AES-256-GCM avec IV aléatoire (FS-S08 / CS-16 / CPA-14..CPA-15) — classe `TotpSecretConverter`
- [x] La clé AES-256 est injectée par `${TOTP_ENCRYPTION_KEY}` (jamais en dur ; valeur de dev dans application.yml, remplacée en prod)
- [x] `password_hash` BCrypt inchangé — ne nécessite pas de chiffrement supplémentaire
- [x] La clé ne figure ni dans les logs, ni dans les réponses API, ni dans les migrations Flyway
| Terminée

|===

== Matrice de traçabilité FS → User Stories

[cols="1,2,2", options="header"]
|===
| Fonction SA | Description | User Stories

| FS-A01 | Créer, suspendre et supprimer les comptes | US-ADM-01, US-ADM-02, US-ADM-03, US-ADM-04
| FS-A02 | Gérer les rôles et droits d'accès | US-ADM-05
| FS-A03 | Gérer les transporteurs | US-ADM-06, US-ADM-07, US-ADM-08, US-ADM-09
| FS-B01 | Inscription + envoi email d'activation | US-REG-01
| FS-B02 | Mise à jour du profil acheteur | US-PRF-01
| FS-B11 | Gérer le carnet d'adresses de livraison | US-PRF-03
| FS-B13 | Collecter et gérer le consentement marketing | US-PRF-04
| FS-B03 | Afficher et filtrer le catalogue | US-SHP-01, US-SHP-02
| FS-B14 | Alerte de remise en stock | US-SHP-03
| FS-B04 | Gérer le panier | US-CRT-01
| FS-B05 | Persister et restaurer le panier | US-CRT-02
| FS-B06 | Orchestrer le tunnel d'achat | US-ORD-01, US-ORD-02
| FS-B07 | Soumettre le paiement (carte ou virement) | US-ORD-03, US-ORD-04
| FS-B08 | Afficher le suivi de livraison | US-EXP-02
| FS-B09 | Annuler une commande (acheteur) | US-CAN-01
| FS-B10 | Activer le compte via token | US-REG-02, US-REG-03
| FS-V01 | Mise à jour du profil vendeur | US-PRF-02
| FS-V02 | Gérer le catalogue produits | US-CAT-01, US-CAT-02, US-CAT-03
| FS-V12 | Importer de nouveaux produits depuis un fichier CSV | US-CAT-06
| FS-V13 | Exporter le catalogue au format CSV | US-CAT-07
| FS-V15 | Exporter la liste des acheteurs consentants (mailing) | US-PRF-05
| FS-V03 | Gérer les stocks et alertes | US-CAT-04, US-CAT-05
| FS-V14 | Mise à jour en masse des stocks | US-CAT-08
| FS-V16 | Configurer l'identité visuelle (logo, bannière, couleurs) | US-VIS-01
| FS-V04 | Traiter les commandes reçues | US-VND-01
| FS-V05 | Déclarer l'expédition avec numéro de suivi | US-EXP-01
| FS-V07 | Consulter les rapports de ventes | US-RPT-01
| FS-V08 | Traiter les annulations (avant/après expédition) | US-CAN-02, US-CAN-03, US-CAN-04
| FS-V09 | Confirmer la réception d'un virement bancaire | US-VND-02
| FS-C01 | Afficher le n° de suivi et l'URL transporteur | US-EXP-02
| FS-P01 | Demander une autorisation de paiement | US-ORD-03
| FS-P02 | Confirmer / refuser la transaction | US-ORD-03, US-ORD-05
| FS-P03 | Déclencher un remboursement | US-CAN-02, US-CAN-03, US-CAN-04
| FS-A05 | Basculer en mode maintenance | US-ADM-10
| FS-V11 | Gérer les annonces défilantes | US-ANN-01
| FS-B09 (acheteur post-expédition) | Demander annulation après expédition | US-CAN-06
| FS-S01 | Politique de mots de passe | US-SEC-01 (partiellement)
| FS-S03 | Gestion des sessions et révocation | US-SEC-01
| FS-S05 | Protection CSRF / SameSite | US-SEC-02
| FS-S06 | En-têtes de sécurité HTTP / TLS | US-SEC-03
| FS-S07 | Journalisation des événements sécurité | US-SEC-05, US-SEC-06
| FS-S11 | Révocation en masse des mots de passe | US-SEC-04
| FS-S08 | MFA Vendeur/Admin (chiffrement secret TOTP) | US-SEC-07
|===
