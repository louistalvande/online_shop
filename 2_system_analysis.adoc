= Analyse Système (SA — System Analysis)
:doctype: article
:toc: left
:toclevels: 3
:sectnums:
:icons: font
:source-highlighter: rouge
:imagesdir: images
:plantuml-server-url: http://www.plantuml.com/plantuml

== Contexte et objectif

L'Analyse Système (SA) décrit le système de boutique en ligne du point de vue des *fonctions que le logiciel doit remplir*, sans encore choisir de composants d'implémentation. Elle traduit les besoins opérationnels (OA) en exigences système, en identifiant les fonctions du système, leurs interfaces avec les acteurs externes et les contraintes techniques à respecter.

La SA repose sur les trois principes ARCADIA suivants :

* Décrire *ce que* le système fait, non *comment* il le fait.
* Allouer chaque besoin opérationnel à une ou plusieurs fonctions système.
* Identifier les échanges entre le système et les acteurs externes (flux, données, événements).

NOTE: Le système couvre l'ensemble des interactions numériques entre les cinq acteurs de la boutique : Administrateur, Vendeur, Acheteur, Transporteur (externe) et Prestataire de paiement (externe). Les deux acteurs externes sont des systèmes tiers ; le système ne les implémente pas, mais définit les interfaces d'échange avec eux.

=== Acteurs système et interfaces

[cols="1,1,2,2", options="header"]
|===
| Acteur | Type | Rôle système | Interfaces principales

| *Administrateur*
| Humain — interne
| Gestion des comptes et des transporteurs
| Interface d'administration back-office (web)

| *Vendeur*
| Humain — interne
| Exploitation commerciale de la boutique
| Interface vendeur back-office (web)

| *Acheteur*
| Humain — externe
| Navigation, achat, suivi de commande
| Interface acheteur front-office (web responsive / mobile)

| *Transporteur*
| Système externe
| Prise en charge et livraison des colis
| API de suivi et de mise à jour de statut (IFS-05)

| *Prestataire de paiement*
| Système externe
| Autorisation et confirmation des transactions
| API de paiement carte / virement (IFS-04)
|===

== Contraintes système

Les contraintes opérationnelles (COp) de l'OA sont traduites en contraintes système (CS) imposées au logiciel.

[cols="1,1,3", options="header"]
|===
| Identifiant | Contrainte système | Description

| CS-01
| Interface responsive
| Le système doit fournir une interface adaptée aux résolutions desktop et mobile (min. 320 px) sans dégradation fonctionnelle — issu de COp-01.

| CS-02
| Modes de paiement
| Le système doit proposer deux modes de paiement lors du tunnel d'achat : **carte bancaire** et **virement bancaire**, via l'API du prestataire externe — issu de COp-02.

| CS-03
| Persistance du panier
| Le système doit persister le panier d'un acheteur authentifié en base et le restaurer intégralement à chaque reconnexion — issu de COp-03.

| CS-04
| Livraison UE uniquement
| Le système doit restreindre la saisie d'adresse de livraison aux pays membres de l'Union Européenne.

| CS-05
| Intégration transporteur
| Le système doit s'interfacer avec au moins un transporteur externe via une API de remise de colis et de suivi de statut.

| CS-06
| Journalisation financière
| Le système doit journaliser toutes les transactions financières (paiement, remboursement) à des fins d'audit.

| CS-07
| Activation de compte par email
| À l'issue de l'inscription, le système doit envoyer automatiquement un email contenant un lien d'activation tokenisé à usage unique. Le compte est activé uniquement sur clic de ce lien. Le token doit avoir une durée de validité limitée (ex. 24 h) au-delà de laquelle un renvoi est possible.
|===

== Exigences fonctionnelles système

Les fonctions système (FS) sont dérivées des cas d'utilisation opérationnels de l'OA et allouées à chaque acteur.

=== Gestion des comptes (Administrateur)

[cols="1,3,1", options="header"]
|===
| ID | Fonction système | Origine UC (OA)

| FS-A01 | Créer, suspendre et supprimer les comptes vendeur et acheteur | UC_A1
| FS-A02 | Gérer les droits d'accès (rôles) sur la plateforme | UC_A1
| FS-A03 | Ajouter, modifier et supprimer les transporteurs disponibles | UC_A2
|===

=== Gestion de la boutique (Vendeur)

[cols="1,3,1", options="header"]
|===
| ID | Fonction système | Origine UC (OA)

| FS-V01 | Permettre au vendeur de mettre à jour ses données de compte | UC_V0
| FS-V02 | Gérer le catalogue produits — CRUD : photos, descriptions, prix, catégories | UC_V1
| FS-V03 | Gérer les niveaux de stock et alerter en cas de rupture imminente | UC_V2
| FS-V04 | Recevoir et traiter les nouvelles commandes (notification, vérification stock) | UC_V3
| FS-V05 | Expédier les commandes vers les pays de l'UE et remettre au transporteur | UC_V4
| FS-V06 | Traiter les litiges et réclamations (remboursement, retour) | UC_V5
| FS-V07 | Consulter les rapports de ventes (CA, produits, historique) | UC_V6
| FS-V08 | Accepter ou refuser une annulation de commande selon son statut d'expédition | UC_V7
|===

=== Expérience d'achat (Acheteur)

[cols="1,3,1", options="header"]
|===
| ID | Fonction système | Origine UC (OA)

| FS-B01 | Gérer l'inscription de l'acheteur et envoyer un email d'activation avec lien tokenisé | UC_B0
| FS-B10 | Valider et activer le compte acheteur sur clic du lien d'activation (token à usage unique, expirant) | UC_B0
| FS-B02 | Permettre à l'acheteur de mettre à jour ses données de compte | UC_B_ACC
| FS-B03 | Afficher et filtrer le catalogue produits disponibles | UC_B1
| FS-B04 | Gérer le panier (ajout, modification de quantité, suppression) | UC_B2
| FS-B05 | Sauvegarder et restaurer le panier entre sessions (CS-03) | UC_B2B
| FS-B06 | Orchestrer le tunnel d'achat (adresse, mode de livraison, validation) | UC_B3 / UC_B4B
| FS-B07 | Soumettre le paiement au prestataire externe — carte ou virement (CS-02) | UC_B4
| FS-B08 | Afficher le suivi de livraison en temps réel (numéro de suivi, statuts) | UC_B5 / UC_C3
| FS-B09 | Permettre l'annulation d'une commande avant expédition | UC_B6
|===

=== Interfaces externes

[cols="1,3,1", options="header"]
|===
| ID | Fonction système | Origine UC (OA)

| FS-P01 | Soumettre une demande d'autorisation de transaction au prestataire de paiement | UC_P1
| FS-P02 | Recevoir la confirmation ou le refus de transaction et notifier les parties | UC_P2
| FS-P03 | Déclencher et confirmer un remboursement via le prestataire | UC_P3
| FS-C01 | Transmettre les données de colis au transporteur lors de l'expédition | UC_C1
| FS-C02 | Recevoir et afficher les mises à jour de statut de livraison du transporteur | UC_C2 / UC_C3
|===

== Diagramme de cas d'utilisation système

Ce diagramme présente les cas d'utilisation système (UCSA), dérivés des cas opérationnels, du point de vue des interactions avec le système logiciel.

[plantuml, sa-use-case, svg]
----
!define FONTSIZE 9
skinparam fontSize FONTSIZE
skinparam actorStyle awesome
skinparam usecase {
  BackgroundColor #FEFEFE
  BorderColor #2E75B6
  ArrowColor #2E75B6
}
skinparam actor {
  BackgroundColor #E8F4FD
  BorderColor #2E75B6
}

left to right direction

actor "Administrateur" as Admin
actor "Vendeur" as Vendor
actor "Acheteur" as Buyer
actor "Transporteur\n(système externe)" as Carrier
actor "Prestataire\nde paiement" as Payment

rectangle "Système — Boutique en ligne" {

  package "Gestion des comptes" {
    usecase (UCSA-12\nGérer les comptes\nutilisateurs et transporteurs) as UC12
  }

  package "Gestion de la boutique" {
    usecase (UCSA-02\nGérer son compte) as UC02
    usecase (UCSA-10\nGérer le catalogue\net les stocks) as UC10
    usecase (UCSA-07\nPréparer et expédier\nune commande) as UC07
    usecase (UCSA-09\nTraiter un litige\nou une réclamation) as UC09
    usecase (UCSA-11\nConsulter les rapports\nde ventes) as UC11
  }

  package "Expérience d'achat" {
    usecase (UCSA-01\nS'inscrire et activer\nson compte via email) as UC01
    usecase (UCSA-03\nParcourir et rechercher\nle catalogue) as UC03
    usecase (UCSA-04\nGérer le panier\net sa persistance) as UC04
    usecase (UCSA-05\nPasser une commande\n(tunnel d'achat)) as UC05
    usecase (UCSA-06\nAnnuler une commande\navant expédition) as UC06
    usecase (UCSA-08\nSuivre la livraison\nd'un colis) as UC08
  }

  package "Interfaces externes" {
    usecase (Autoriser / Confirmer\nune transaction) as UC_P
    usecase (Remettre un colis /\nSuivre le statut) as UC_C
  }
}

Admin --> UC12

Vendor --> UC02
Vendor --> UC10
Vendor --> UC07
Vendor --> UC09
Vendor --> UC11
Vendor --> UC06 : accepte/refuse

Buyer --> UC01
Buyer --> UC02
Buyer --> UC03
Buyer --> UC04
Buyer --> UC05
Buyer --> UC06
Buyer --> UC08

UC05 --> UC_P : IFS-04
UC09 --> UC_P : remboursement
UC07 --> UC_C : IFS-05
UC08 --> UC_C : statuts

Payment --> UC_P
Carrier --> UC_C
----

== Cas d'utilisation système — Descriptions détaillées

=== UCSA-01 : S'inscrire et activer son compte via email

[cols="1,3", options="header"]
|===
| Champ | Description

| *Identifiant*
| UCSA-01

| *Acteurs*
| Acheteur (principal) ; Service de notification email — IFS-06 (secondaire)

| *Préconditions*
| Aucun compte existant avec cette adresse email

| *Déclencheur*
| L'acheteur soumet le formulaire d'inscription

| *Scénario nominal*
a|
. L'acheteur saisit son email, son mot de passe et ses informations de compte.
. Le système crée le compte avec le statut « en attente d'activation » (FS-B01).
. Le système génère un token d'activation à usage unique avec une durée de validité de 24 h (CS-07).
. Le système envoie un email contenant le lien d'activation tokenisé (IFS-06 / FS-B10).
. L'acheteur clique sur le lien depuis sa messagerie.
. Le système vérifie la validité du token (non expiré, non déjà utilisé).
. Le système active le compte et redirige l'acheteur vers la page de connexion.

| *Scénarios alternatifs*
a|
* *6a.* Le token est expiré (> 24 h) : le système retourne une erreur et propose un renvoi du lien.
* *6b.* Le token a déjà été utilisé : le système affiche « compte déjà activé ».
* *2a.* L'adresse email est déjà utilisée : le système retourne une erreur sans créer de doublon.

| *Postconditions*
| Compte acheteur actif ; l'acheteur peut s'authentifier et accéder aux fonctionnalités d'achat.

| *Exigences liées*
| FS-B01, FS-B10, CS-07, IFS-06
|===

=== UCSA-05 : Passer une commande (tunnel d'achat)

[cols="1,3", options="header"]
|===
| Champ | Description

| *Identifiant*
| UCSA-05

| *Acteurs*
| Acheteur (principal) ; Prestataire de paiement (secondaire)

| *Préconditions*
| Acheteur authentifié ; panier non vide ; adresse de livraison dans un pays de l'UE

| *Déclencheur*
| L'acheteur clique sur « Valider le panier »

| *Scénario nominal*
a|
. Le système affiche le récapitulatif du panier.
. L'acheteur saisit ou sélectionne une adresse de livraison (pays UE — CS-04).
. Le système affiche les modes de livraison disponibles et leurs tarifs.
. L'acheteur choisit un mode de livraison.
. L'acheteur saisit ses informations de paiement (carte ou virement — CS-02).
. Le système soumet la demande au prestataire de paiement (IFS-04 / FS-P01).
. Le prestataire autorise la transaction.
. Le système confirme la commande, réserve le stock et notifie l'acheteur et le vendeur.

| *Scénarios alternatifs*
a|
* *6a.* Le prestataire refuse la transaction : le système notifie l'acheteur et l'invite à réessayer ou à changer de mode de paiement.
* *2a.* L'adresse de livraison est hors UE : le système affiche un message d'erreur et bloque la validation.

| *Postconditions*
| Commande créée en base ; paiement autorisé ; vendeur notifié ; stock réservé.

| *Exigences liées*
| FS-B06, FS-B07, FS-P01, FS-P02, CS-02, CS-04
|===

=== UCSA-06 : Annuler une commande avant expédition

[cols="1,3", options="header"]
|===
| Champ | Description

| *Identifiant*
| UCSA-06

| *Acteurs*
| Acheteur (principal) ; Vendeur (secondaire) ; Prestataire de paiement (secondaire)

| *Préconditions*
| Commande existante dans l'état « en préparation » (non encore remise au transporteur)

| *Déclencheur*
| L'acheteur soumet une demande d'annulation depuis son espace commandes

| *Scénario nominal*
a|
. Le système transmet la demande d'annulation au vendeur.
. Le vendeur vérifie que le colis n'a pas encore été remis au transporteur.
. Le vendeur accepte l'annulation dans le système.
. Le système déclenche un remboursement via le prestataire (IFS-04 / FS-P03).
. Le prestataire confirme le remboursement.
. Le système notifie l'acheteur de l'annulation et du remboursement.

| *Scénarios alternatifs*
a|
* *2a.* Le colis est déjà remis au transporteur : le vendeur refuse l'annulation ; le système notifie l'acheteur du refus.

| *Postconditions*
| Commande annulée en base ; remboursement initié ; stock libéré.

| *Exigences liées*
| FS-B09, FS-V08, FS-P03
|===

== Diagramme d'activité système — Flux de commande complet

Ce diagramme modélise le flux système de bout en bout, depuis la connexion de l'acheteur jusqu'à la clôture de la commande, en faisant apparaître les échanges avec les systèmes externes.

[plantuml, sa-activity, svg]
----
skinparam activityBackgroundColor #FEFEFE
skinparam activityBorderColor #2E75B6
skinparam arrowColor #2E75B6
skinparam swimlaneBorderColor #AAAAAA

|Acheteur|
start
:S'inscrire (FS-B01);

|Système|
:Générer token d'activation (CS-07);
:Envoyer email avec lien tokenisé\n(IFS-06 / FS-B10);

|Acheteur|
:Cliquer sur le lien d'activation;

|Système|
if (Token valide et non expiré ?) then (oui)
  :Activer le compte (FS-B10);
else (non)
  :Notifier l'acheteur — token expiré;
  :Proposer le renvoi du lien;
  stop
endif

|Acheteur|
:S'authentifier (FS-B01);

|Système|
:Restaurer le panier\nsauvegardé (FS-B05 / CS-03);

|Acheteur|
:Parcourir le catalogue (FS-B03);
:Ajouter des produits au panier (FS-B04);
:Valider le panier;

|Système|
:Afficher récapitulatif;
:Contrôler adresse de livraison UE (CS-04);

|Acheteur|
:Saisir adresse de livraison\n+ choisir mode de livraison;
:Soumettre le paiement\n(carte ou virement — CS-02);

|Système|
:Soumettre la demande\nau prestataire (FS-P01 / IFS-04);

|Prestataire de paiement|
if (Transaction autorisée ?) then (oui)
  :Confirmer la transaction (FS-P02);
else (non)
  :Refuser la transaction;

  |Système|
  :Notifier l'acheteur\nde l'échec;
  stop
endif

|Système|
:Créer la commande en base;
:Réserver le stock;
:Notifier l'acheteur (confirmation);
:Notifier le vendeur (nouvelle commande);

|Acheteur|
if (Annulation souhaitée ?) then (oui)
  :Demander l'annulation (FS-B09);

  |Système|
  :Transmettre la demande\nau vendeur (FS-V08);

  |Vendeur|
  if (Commande déjà expédiée ?) then (oui)
    :Refuser l'annulation;

    |Système|
    :Notifier l'acheteur du refus;
  else (non)
    :Accepter l'annulation;

    |Système|
    :Déclencher le remboursement (FS-P03 / IFS-04);

    |Prestataire de paiement|
    :Confirmer le remboursement;

    |Système|
    :Notifier l'acheteur;
    :Libérer le stock;
    stop
  endif
else (non)
endif

|Vendeur|
:Vérifier disponibilité du stock (FS-V03);

if (Stock disponible ?) then (oui)
  :Préparer la commande;
else (non)
  :Alerter l'acheteur du délai;
  :Réapprovisionner le stock;
  :Préparer la commande;
endif

:Déclarer l'expédition (FS-V05);

|Système|
:Transmettre le colis\nau transporteur (FS-C01 / IFS-05);

|Transporteur|
:Prendre en charge le colis;
:Acheminer vers le pays\nde l'acheteur (UE);
:Mettre à jour les statuts\nde livraison (webhook);

|Système|
:Recevoir les statuts (FS-C02);
:Notifier l'acheteur\n(n° de suivi + statut — FS-B08);

|Acheteur|
:Consulter le suivi de livraison;
:Réceptionner le colis;

|Système|
:Clôturer la commande;
:Mettre à jour les rapports\nde ventes (FS-V07);

stop
----

== Diagramme de séquence système — Passation de commande

Ce diagramme représente les échanges chronologiques entre les acteurs et le système lors du flux de commande nominal, en mettant en évidence les interfaces externes (IFS-04 et IFS-05).

[plantuml, sa-sequence, svg]
----
skinparam sequenceArrowColor #2E75B6
skinparam sequenceParticipantBackgroundColor #E8F4FD
skinparam sequenceParticipantBorderColor #2E75B6
skinparam sequenceLifeLineBorderColor #2E75B6
skinparam noteBorderColor #F0A500
skinparam noteBackgroundColor #FFFBE6

actor Acheteur as Buyer
participant "Système\n(Boutique)" as Sys
actor Vendeur as Vendor
actor Administrateur as Admin
participant "IFS-05\nTransporteur" as Carrier
participant "IFS-04\nPrestataire paiement" as Payment

== Inscription et activation de compte (UCSA-01) ==
Buyer -> Sys : POST /register {email, mdp, adresse}
Sys --> Buyer : 201 Created — compte créé, statut "en attente d'activation"
Sys -> Sys : Générer token d'activation (UUID, TTL 24h)
Sys --> Buyer : IFS-06 — Email : lien d'activation\nhttps://boutique/activate?token={uuid}
note right : FS-B01 / FS-B10 / CS-07\nToken à usage unique, expire dans 24 h

Buyer -> Sys : GET /activate?token={uuid}
Sys -> Sys : Vérifier token (valide + non expiré)
Sys --> Buyer : 200 OK — compte activé, redirection vers connexion
note right : Si token expiré :\nSys --> Buyer : 410 Gone\nBuyer peut demander un renvoi

== Navigation et gestion du panier (UCSA-03 / UCSA-04) ==
Buyer -> Sys : GET /catalogue?filtre=...
Sys --> Buyer : 200 OK — liste produits (FS-B03)

Buyer -> Sys : POST /panier/lignes {produit_id, qté}
Sys --> Buyer : 200 OK — panier mis à jour (FS-B04)
note right : Panier sauvegardé en base\npour la prochaine session (CS-03)

== Tunnel d'achat et paiement (UCSA-05) ==
Buyer -> Sys : POST /commandes {panier_id, adresse_UE, mode_livraison, mode_paiement}
note right : CS-04 — adresse validée UE\nCS-02 — carte ou virement

Sys -> Payment : POST /authorize {montant, mode, références}
note right : IFS-04 (FS-P01)

alt Paiement autorisé
  Payment --> Sys : 200 OK — {transaction_id, statut: "autorisé"}
  Sys --> Buyer : 201 Created — commande confirmée {commande_id}
  Sys -> Vendor : Notification : nouvelle commande #{commande_id}

  == Préparation et expédition (UCSA-07) ==
  Vendor -> Sys : PUT /commandes/{id}/statut {statut: "en_préparation"}
  Sys --> Buyer : Notification : commande en préparation

  opt Annulation demandée avant expédition (UCSA-06)
    Buyer -> Sys : POST /commandes/{id}/annulation
    Sys -> Vendor : Notification : demande d'annulation
    alt Commande non expédiée
      Vendor -> Sys : PUT /commandes/{id}/annulation {accepté: true}
      Sys -> Payment : POST /refunds {transaction_id, montant}
      Payment --> Sys : 200 OK — remboursement confirmé (FS-P03)
      Sys --> Buyer : Notification : annulation + remboursement confirmé
    else Commande déjà expédiée
      Vendor -> Sys : PUT /commandes/{id}/annulation {accepté: false}
      Sys --> Buyer : Notification : annulation refusée (colis déjà expédié)
    end
  end

  Vendor -> Sys : PUT /commandes/{id}/expedition {transporteur_id, pays_destination}
  Sys -> Carrier : POST /shipments {colis, adresse, service}
  note right : IFS-05 (FS-C01)
  Carrier --> Sys : 201 Created — {tracking_number}
  Sys --> Buyer : Notification : expédié — n° de suivi {tracking_number}

  == Suivi de livraison (UCSA-08) ==
  Carrier -> Sys : POST /webhooks/tracking {tracking_number, statut, timestamp}
  note right : IFS-05 (FS-C02)
  Sys --> Buyer : Notification : mise à jour statut livraison

  Buyer -> Sys : GET /commandes/{id}/suivi
  Sys --> Buyer : 200 OK — statuts d'acheminement

  Buyer -> Sys : PUT /commandes/{id}/reception {confirmé: true}
  Sys --> Vendor : Notification : réception confirmée

  Vendor -> Sys : GET /rapports/ventes
  Sys --> Vendor : 200 OK — statistiques et historique (FS-V07)

else Paiement refusé
  Payment --> Sys : 402 Payment Required — {raison}
  Sys --> Buyer : Notification : échec paiement — réessayer ou changer de mode
  note right : L'acheteur peut retenter\nou abandonner la commande
end
----

== Diagramme de classes logique — Modèle de données système

Ce diagramme représente les entités manipulées par le système, leurs attributs principaux et leurs relations. Il constitue le modèle de données logique de la SA, indépendant de toute technologie de persistance.

[plantuml, sa-classes, svg]
----
skinparam classBackgroundColor #FEFEFE
skinparam classBorderColor #2E75B6
skinparam arrowColor #2E75B6
skinparam noteBackgroundColor #FFFBE6
skinparam noteBorderColor #F0A500

class CompteUtilisateur {
  + id : UUID
  + email : String
  + motDePasseHaché : String
  + rôle : Enum {admin, vendeur, acheteur}
  + statut : Enum {actif, suspendu, supprimé}
  + dateCreation : DateTime
}

class Produit {
  + id : UUID
  + nom : String
  + description : Text
  + prixHT : Decimal
  + photos : List<URL>
  + catégorie : String
  + statut : Enum {publié, archivé}
}

class Stock {
  + id : UUID
  + quantitéDisponible : Integer
  + seuilAlerte : Integer
}

class Panier {
  + id : UUID
  + dateMiseAJour : DateTime
}

class LignePanier {
  + quantité : Integer
  + prixUnitaireCapturé : Decimal
}

class Commande {
  + id : UUID
  + adresseLivraison : Adresse
  + modeLivraison : String
  + montantTotal : Decimal
  + statut : Enum {en_attente, en_préparation,\nexpédiée, livrée, annulée}
  + dateCommande : DateTime
}

class Paiement {
  + id : UUID
  + montant : Decimal
  + mode : Enum {carte, virement}
  + statut : Enum {autorisé, refusé, remboursé}
  + référencePrestataire : String
  + date : DateTime
}

class Expédition {
  + id : UUID
  + numéroSuivi : String
  + statutLivraison : String
  + dateExpédition : DateTime
  + dateLivraisonEstimée : Date
}

class Transporteur {
  + id : UUID
  + nom : String
  + paysSupported : List<CodeISO>
  + urlApiSuivi : URL
  + actif : Boolean
}

class Réclamation {
  + id : UUID
  + motif : String
  + statut : Enum {ouverte, résolue, clôturée}
  + dateOuverture : DateTime
}

class Adresse {
  + rue : String
  + ville : String
  + codePostal : String
  + paysUE : CodeISO
}

CompteUtilisateur "1" --> "0..1" Panier : possède
CompteUtilisateur "1" --> "0..*" Commande : passe
Panier "1" *-- "0..*" LignePanier : contient
LignePanier "0..*" --> "1" Produit : référence
Produit "1" -- "1" Stock : géré par
Commande "1" *-- "1" Paiement : réglée par
Commande "1" *-- "0..1" Expédition : donne lieu à
Commande "1" --> "0..*" Réclamation : fait l'objet de
Commande "1" --> "1" Adresse : livrée à
Expédition "0..*" --> "1" Transporteur : acheminée par

note bottom of Commande
  CS-04 : adresse.paysUE
  doit être dans l'UE
end note

note bottom of Paiement
  CS-02 : mode ∈ {carte, virement}
  CS-06 : tous les paiements sont journalisés
end note
----

== Interfaces système (IFS)

[cols="1,2,1,2", options="header"]
|===
| Identifiant | Interface | Type / Canal | Données échangées

| *IFS-01*
| Portail acheteur (front-office)
| Web responsive HTTPS (CS-01)
| Catalogue, panier, tunnel d'achat, suivi commande, profil

| *IFS-02*
| Back-office vendeur
| Web HTTPS — desktop-first
| Catalogue, stocks, commandes, expéditions, rapports de ventes

| *IFS-03*
| Back-office administrateur
| Web HTTPS — desktop-first
| Comptes utilisateurs, transporteurs, logs d'audit

| *IFS-04*
| API prestataire de paiement
| REST/HTTPS — appel sortant + webhook entrant
| Demande d'autorisation, confirmation de transaction, déclenchement de remboursement

| *IFS-05*
| API transporteur
| REST/HTTPS — appel sortant + webhook entrant
| Remise de colis, numéro de suivi, mises à jour de statut d'acheminement

| *IFS-06*
| Notifications email / SMS
| SMTP / SMS Gateway — sortant
| Confirmation de commande, expédition, annulation, alerte de rupture de stock
|===

== Diagramme de composants système

Ce diagramme identifie les grands blocs fonctionnels du système et leurs dépendances, sans préjuger de l'architecture technique retenue lors de la LA (Logical Architecture).

[plantuml, sa-components, svg]
----
skinparam componentBackgroundColor #FEFEFE
skinparam componentBorderColor #2E75B6
skinparam arrowColor #2E75B6
skinparam interfaceBackgroundColor #E8F4FD
skinparam interfaceBorderColor #2E75B6
skinparam noteBackgroundColor #FFFBE6
skinparam noteBorderColor #F0A500
skinparam packageBackgroundColor #F0F8FF
skinparam packageBorderColor #AAAAAA

actor "Acheteur" as Buyer
actor "Vendeur" as Vendor
actor "Administrateur" as Admin

package "Système — Boutique en ligne" {

  package "Front-office (IFS-01)" {
    [Portail Acheteur\n(catalogue, panier, tunnel d'achat)] as FrontBuyer
  }

  package "Back-office (IFS-02 / IFS-03)" {
    [Espace Vendeur\n(catalogue, stock, commandes, rapports)] as BackVendor
    [Console Admin\n(comptes, transporteurs)] as BackAdmin
  }

  package "Noyau Métier" {
    [Gestion des comptes\n(FS-A01..A04, FS-B01..B02)] as AccountSvc
    [Gestion du catalogue\net des stocks (FS-V02..V03, FS-B03)] as CatalogSvc
    [Gestion du panier\n(FS-B04..B05 / CS-03)] as CartSvc
    [Orchestrateur de commande\n(FS-B06..B09, FS-V04..V08)] as OrderSvc
    [Moteur de rapports\n(FS-V07)] as ReportSvc
  }

  package "Intégrations externes" {
    [Connecteur Paiement\n(FS-P01..P03 / IFS-04)] as PaySvc
    [Connecteur Transporteur\n(FS-C01..C02 / IFS-05)] as CarrierSvc
    [Service Notifications\n(email / SMS / IFS-06)] as NotifSvc
  }

}

component "Prestataire\nde paiement" as ExtPayment #D5E8F0
component "Transporteur\n(UE)" as ExtCarrier #D5E8F0

Buyer --> FrontBuyer : IFS-01
Vendor --> BackVendor : IFS-02
Admin --> BackAdmin : IFS-03

FrontBuyer --> AccountSvc
FrontBuyer --> CatalogSvc
FrontBuyer --> CartSvc
FrontBuyer --> OrderSvc

BackVendor --> CatalogSvc
BackVendor --> OrderSvc
BackVendor --> ReportSvc

BackAdmin --> AccountSvc

OrderSvc --> PaySvc
OrderSvc --> CarrierSvc
OrderSvc --> NotifSvc
AccountSvc --> NotifSvc
CatalogSvc --> NotifSvc

PaySvc --> ExtPayment : IFS-04\nREST/HTTPS
ExtPayment --> PaySvc : webhook

CarrierSvc --> ExtCarrier : IFS-05\nREST/HTTPS
ExtCarrier --> CarrierSvc : webhook statuts
----

== Matrice de traçabilité OA → SA

La matrice ci-dessous assure la couverture complète de chaque besoin opérationnel de l'OA par au moins une exigence ou fonction système de la SA.

[cols="1,2,2,1", options="header"]
|===
| UC / COp (OA) | Besoin opérationnel | Fonctions SA | UCSA

| UC_A1 | Gérer les comptes utilisateurs | FS-A01, FS-A02 | UCSA-12
| UC_A2 | Gérer les transporteurs | FS-A03 | UCSA-12
| UC_B0 | S'inscrire / activer son compte via lien email | FS-B01, FS-B10, CS-07 | UCSA-01, UCSA-02
| UC_B_ACC | Mettre à jour son compte (acheteur) | FS-B02 | UCSA-02
| UC_B1 | Parcourir le catalogue | FS-B03 | UCSA-03
| UC_B2 / UC_B2B | Gérer et persister le panier | FS-B04, FS-B05 | UCSA-04
| UC_B3 / UC_B4B | Passer une commande / choisir la livraison | FS-B06 | UCSA-05
| UC_B4 | Effectuer un paiement | FS-B07, FS-P01, FS-P02 | UCSA-05
| UC_B5 / UC_C3 | Suivre la livraison | FS-B08, FS-C02 | UCSA-08
| UC_B6 / UC_V7 | Annuler une commande | FS-B09, FS-V08, FS-P03 | UCSA-06
| UC_V0 | Mettre à jour son compte (vendeur) | FS-V01 | UCSA-02
| UC_V1 | Gérer le catalogue produits | FS-V02 | UCSA-10
| UC_V2 | Gérer les stocks | FS-V03 | UCSA-10
| UC_V3 | Traiter les commandes reçues | FS-V04 | UCSA-07
| UC_V4 / UC_C1 | Expédier vers l'UE / remise transporteur | FS-V05, FS-C01 | UCSA-07
| UC_V5 | Traiter les litiges et réclamations | FS-V06, FS-P03 | UCSA-09
| UC_V6 | Consulter les rapports de ventes | FS-V07 | UCSA-11
| COp-01 | Interface responsive | CS-01 | —
| COp-02 | Modes de paiement carte / virement | CS-02, CS-06 | UCSA-05
| COp-03 | Persistance du panier entre sessions | CS-03, FS-B05 | UCSA-04
| — | Activation de compte par lien email | CS-07, FS-B01, FS-B10 | UCSA-01
|===
